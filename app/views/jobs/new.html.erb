<div class="container">
  <h1 class="mb-4">Post a New Job</h1>

  <div class="card">
    <div class="card-body">
      <%= render 'form', job: @job %>
    </div>
  </div>

  <%= link_to 'Back to Jobs', jobs_path, class: 'btn btn-secondary mt-3' %>
</div>
